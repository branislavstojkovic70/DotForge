{
  "name": "dotforge-contracts",
  "version": "0.2.0",
  "description": "Dotforge PolkaVM contract — pvm-contract-macros",
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10"
  },
  "scripts": {
    "deploy:local": "npx hardhat run scripts/deploy.ts --network local",
    "deploy:testnet": "npx hardhat run scripts/deploy.ts --network polkadotTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-viem": "^2.1.3",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2",
    "viem": "^2.48.2"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
